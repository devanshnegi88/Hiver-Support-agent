pandas>=2.0
scikit-learn>=1.3
# Gemini calls go through HTTPS in src/llm_client.py (stdlib urllib).
# No Gemini SDK required — the google.generativeai / google-genai clients
# send an OAuth Authorization header that Gemini 3.x rejects.
pyarrow>=14.0
numpy>=1.24
pytest>=8.0
